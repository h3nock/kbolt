[package]
name = "kbolt-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
kbolt-core = { path = "../core" }
kbolt-types = { path = "../types" }

[dev-dependencies]
tempfile = "3.26"
