# kbolt-mcp

`kbolt-mcp` exposes `kbolt` functionality through an MCP adapter built on top of `kbolt-core`.

This crate is not the main end-user install target. If you want the command-line application, install the `kbolt` package instead.
